version: '3.7'

services:
  backend:
    image: node:16-alpine
    volumes:
      - ../backend:/app
    depends_on:
      - database
      - flyway
    command: >
      sh -c "cd /app && npm run test -- --watchAll"
